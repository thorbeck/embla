import { Component, h } from '@stencil/core';

@Component({
  tag: 'embla-date-picker',
  styleUrl: 'date-picker.scss',
  shadow: true,
})
export class DatePicker {
  render() {
    return <div>Hello, World!</div>;
  }
}
